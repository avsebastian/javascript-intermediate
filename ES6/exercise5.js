/**
 * Vuelva a escribir la myConcatfunción que agrega contenido de arr2para arr1que la función use 
 * la sintaxis de función de flecha.
 */

 const myConcat = (arr1, arr2) => {
    return arr1.concat(arr2);
  };
  
  console.log(myConcat([1, 2], [3, 4, 5]));