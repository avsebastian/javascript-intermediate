/**
 * Actualicemos la incrementerfunción para declarar claramente sus dependencias.
 * Escriba la incrementerfunción para que tome un argumento y luego devuelva un resultado 
 * después de aumentar el valor en uno.
 */

let fixedValue = 4;

function incrementer(arg) {
let a = arg + 1;
return a;
}